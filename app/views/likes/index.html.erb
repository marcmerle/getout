<div class="likes-index">
  <h3>My List</h3>
  <div class="tabs-search">
    <ul class="nav nav-pills">
      <li class="nav-item">
        <a class="nav-link selected", data: {turbolinks: false}, href="#">Spots <%= icon("fas", "heart") %></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#">Friends</a>
      </li>
    </ul>
  </div>
  <div class="places-likes">
    <% @likes.each do |like| %>
      <%= render "components/likes_card", place: like.place %>
    <% end %>
  </div>
  <div class="tags-filter">
    <h4>Tags</h4>
    <% @genres.uniq.each do |genre|  %>
      <%= genre %>
    <% end %>
  </div>
</div>
