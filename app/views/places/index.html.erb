<%= render 'components/back_arrow', link: root_path %>
<div class="index-container">
  <div class="tabs-search">
    <ul class="nav nav-pills">
      <li class="nav-item">
        <a class="nav-link" href="#">By genres</a>
      </li>
      <li class="nav-item">
        <a class="nav-link selected" href="#">By location</a>
      </li>
    </ul>
  </div>
  <div class="bar-search">
    Search
  </div>
  <div class="filter-search">
    <%= render 'components/tag', text: "Rate", class: "filter-tag" %>
    <%= render 'components/tag', text: "Genre", class: "filter-tag" %>
    <%= render 'components/tag', text: "Category", class: "filter-tag" %>
  </div>
  <div class="search-results mb-3">
    <div class="title-section">
      <h3>Results for #</h3>
    </div>
    <div class="places">
      <%= render "components/search-card", place: @places.sample %>
      <%= render "components/search-card", place: @places.sample %>
      <%= render "components/search-card", place: @places.sample %>
      <%= render "components/search-card", place: @places.sample %>
      <%= render "components/search-card", place: @places.sample %>
      <%= render "components/search-card", place: @places.sample %>
    </div>
  </div>
</div>


